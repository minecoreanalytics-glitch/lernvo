# Local development — copy to backend/.env
DATABASE_URL=postgresql://lernvo:lernvo@127.0.0.1:5432/lernvo
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=
JWT_SECRET=dev-secret-change-me
NODE_ENV=development
PORT=4000
FRONTEND_URL=http://localhost:3000
DOMAIN=localhost
PLATFORM_NAME=Lernvo
# Optional — AI (module/quiz/KB generation + chatbot). Leave empty to disable.
GEMINI_API_KEY=
# Optional — SMTP emails. Leave empty to disable.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
